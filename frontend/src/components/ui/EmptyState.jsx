const EmptyState = ({ title, message }) => (
  <div className="text-center p-8 text-midnight">
    <h2 className="text-lg text-gold font-bold">{title}</h2>
    <p>{message}</p>
  </div>
);
export default EmptyState;
